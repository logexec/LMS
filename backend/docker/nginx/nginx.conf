server {
    listen 80;
    server_name 10.120.1.20;

    root /var/www/html/public;
    index index.php index.html;

    # —————— CORS + Preflight para TODA la API ——————
    location ^~ /api/ {
        # 1) Preflight OPTIONS → 204 + headers
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin   "http://10.120.1.20:3000" always;
            add_header Access-Control-Allow-Credentials "true"                always;
            add_header Access-Control-Allow-Methods     "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
            add_header Access-Control-Allow-Headers     "Content-Type, Accept, Authorization, X-Requested-With, X-XSRF-TOKEN" always;
            return 204;
        }

        # 2) Para GET/POST/PUT/etc → inyecta headers y pasa a PHP
        add_header Access-Control-Allow-Origin   "http://10.120.1.20:3000" always;
        add_header Access-Control-Allow-Credentials "true"                always;
        add_header Access-Control-Allow-Methods     "GET, POST, PUT, PATCH, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers     "Content-Type, Accept, Authorization, X-Requested-With, X-XSRF-TOKEN" always;

        try_files $uri $uri/ /index.php?$query_string;
    }

    # —————— Resto de rutas (assets, páginas web) ——————
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # PHP-FPM
    location ~ \.php$ {
        fastcgi_pass   backend:9000;
        fastcgi_index  index.php;
        include        fastcgi_params;
        fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
        # No pongas aquí add_header
    }

    location ~ /\.ht {
        deny all;
    }
}
